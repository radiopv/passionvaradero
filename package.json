{
  "dependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "lovable-tagger": "^1.0.19"
  },
  "scripts": {
    "build:dev": "vite build --mode development"
  },
  "devDependencies": {
    "vite": "^4.0.0", // Ensure Vite is installed
    "typescript": "^5.0.0" // Ensure TypeScript is installed if you're using TS
  }
}
